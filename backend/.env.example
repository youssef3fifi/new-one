# Server Configuration
PORT=5000
NODE_ENV=development

# CORS Configuration (Update with your EC2 IP or domain)
CORS_ORIGIN=*

# JWT Secret (Optional - for authentication)
JWT_SECRET=your_jwt_secret_key_here
